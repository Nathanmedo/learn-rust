//in rust main() is a predefined function that acts as an entry point to the program
fn main(){
    println!("{}", "Hello, world!");
}

//the main function acts as the entry point so anything written outside isnt read.